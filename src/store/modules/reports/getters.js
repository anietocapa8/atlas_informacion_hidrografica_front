const getReports = (state) => {
    return state.Reports;
}

const getReportsPages = (state) => {
    return state.Reports;
}

const getReportsQuery = (state) => {
    return state.ReportsQuery;
}

const getReportsMeta = (state) => {
    return state.ReportsTotalRegisters;
}

export default {
    getReports,
    getReportsPages,
    getReportsQuery,
    getReportsMeta
}