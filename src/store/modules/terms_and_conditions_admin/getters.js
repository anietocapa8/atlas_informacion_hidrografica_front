const getTermsAndConditions = (state) => {
  return state.termsAndConditions;
}

const getTermsAndConditionsPages = (state) => {
  return state.termsAndConditions;
}
 
const getTermsAndConditionsQuery = (state) => {
  return state.termsAndConditionsQuery;
}

const getTermsAndConditionsMeta = (state) => {
  return  state.termsAndConditionsTotalRegisters;
}
  
export default {
  getTermsAndConditions,
  getTermsAndConditionsPages,
  getTermsAndConditionsQuery,
  getTermsAndConditionsMeta
}